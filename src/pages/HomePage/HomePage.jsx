import HeroSection from '../../components/HeroSection/HeroSection';

const HomePage = () => {
    return (
        <section>
            <HeroSection />
        </section>
    );
};

export default HomePage;
